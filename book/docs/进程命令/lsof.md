# lsof

执行下面的命令，查看当前系统中全部文件与进程对应的信息。

```
sudo lsof
```

执行下面的命令，查看调用指定文件的所有进程信息。

```
lsof /bin/bash
```

执行下面的命令，查看指定用户打开的文件信息。

```
lsof -u kuriv
```

执行下面的命令，查看指定进程打开的文件信息。

```
lsof -c bash
```

执行下面的命令，查看指定进程号打开的文件信息。

```
lsof -p 80
```


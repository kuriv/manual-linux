# uname

执行下面的命令，显示系统主机名、内核版本、硬件架构等信息。

```
uname -a
```

执行下面的命令，仅显示系统主机名。

```
uname -n
```

执行下面的命令，仅显示内核版本。

```
uname -r
```

执行下面的命令，仅显示内核名称。

```
uname -s
```

执行下面的命令，仅显示当前系统的硬件平台。

```
uname -i
```

执行下面的命令，仅显示系统类型。

```
uname -o
```


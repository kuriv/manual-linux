# chown

执行下面的命令，改变指定文件的所属用户和组。

```
sudo chown kuriv:kuriv file.txt
```

执行下面的命令，改变指定文件的所属用户。

```
sudo chown kuriv file.txt
```

执行下面的命令，改变指定文件的所属组。

```
sudo chown :kuriv file.txt
```

执行下面的命令，改变指定目录及其所有子文件的所属用户和组。

```
sudo chown -R kuriv:kuriv dir
```


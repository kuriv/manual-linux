# history

执行下面的命令，显示执行过的全部命令记录。

```
history
```

执行下面的命令，显示最近执行过的指定数量命令记录。

```
history 5
```

执行下面的命令，重新执行指定命令。

```
history !1900
```

执行下面的命令，删除指定命令。

```
history -d 1900
```

执行下面的命令，清空全部历史记录。

```
history -c
```

